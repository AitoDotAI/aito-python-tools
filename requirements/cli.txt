# The `aitoai[cli]` extra: the command-line tool, schema inference and file conversion.
# This is the ~100 MB dataframe toolchain a plain API caller should not have to install.
pandas ~= 2.0
langdetect >= 1.0.7, < 2.0
argcomplete >= 1.11.1, < 4.0
xlrd >= 1.1, < 3.0
openpyxl >= 3.0.7
fastparquet >= 0.8

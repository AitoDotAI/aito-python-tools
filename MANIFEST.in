# setup.py reads these at build time; without them an sdist cannot be installed.
# (Every sdist up to 0.6.3 shipped without them and fails with FileNotFoundError.)
include requirements/base.txt
include requirements/cli.txt
include README.rst
include LICENSE
